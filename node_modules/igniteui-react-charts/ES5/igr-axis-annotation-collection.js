import * as tslib_1 from "tslib";
import { IgCollection } from "igniteui-react-core/ES5/IgCollection";
import { markType } from "igniteui-react-core/ES5/type";
import { SyncableObservableCollection$2 } from "igniteui-react-core/ES5/SyncableObservableCollection$2";
import { IAxisAnnotation_$type } from './IAxisAnnotation';
import { AxisAnnotation } from './igr-axis-annotation';
var AxisAnnotationCollection = /** @class */ (function (_super) {
    tslib_1.__extends(AxisAnnotationCollection, _super);
    function AxisAnnotationCollection(list) {
        var _this = _super.call(this) || this;
        if (!AxisAnnotation.$type) {
            AxisAnnotation.$type = markType(AxisAnnotation, "AxisAnnotation");
        }
        if (list) {
            for (var i = 0; i < list.length; i++) {
                _this.add(list[i]);
            }
        }
        return _this;
    }
    AxisAnnotationCollection.prototype._createInnerColl = function () {
        if (!AxisAnnotation.$type) {
            AxisAnnotation.$type = markType(AxisAnnotation, "AxisAnnotation");
        }
        var coll = new SyncableObservableCollection$2(AxisAnnotation.$type, IAxisAnnotation_$type, 0);
        coll.compare = function (ext, int) {
            var comp = ext;
            if (comp._implementation) {
                comp = comp._implementation;
            }
            if (comp.equals) {
                return comp.equals(int);
            }
            return comp === int;
        };
        coll.createTo = function (ext) {
            return ext._implementation;
        };
        coll.createFrom = function (int) {
            var ext = int.externalObject;
            if (!ext) {
                ext = new AxisAnnotation();
                ext._implementation = int;
            }
            return ext;
        };
        return coll;
    };
    return AxisAnnotationCollection;
}(IgCollection));
export { AxisAnnotationCollection };
