import * as tslib_1 from "tslib";
import { IgCollection } from "igniteui-react-core/ES5/IgCollection";
import { markType } from "igniteui-react-core/ES5/type";
import { SyncableObservableCollection$2 } from "igniteui-react-core/ES5/SyncableObservableCollection$2";
import { CalloutAnnotation as CalloutAnnotation_internal } from './CalloutAnnotation';
import { CalloutAnnotation as CalloutAnnotation } from './igr-callout-annotation';
var CalloutAnnotationCollection = /** @class */ (function (_super) {
    tslib_1.__extends(CalloutAnnotationCollection, _super);
    function CalloutAnnotationCollection(list) {
        var _this = _super.call(this) || this;
        if (!CalloutAnnotation.$type) {
            CalloutAnnotation.$type = markType(CalloutAnnotation, "CalloutAnnotation");
        }
        if (list) {
            for (var i = 0; i < list.length; i++) {
                _this.add(list[i]);
            }
        }
        return _this;
    }
    CalloutAnnotationCollection.prototype._createInnerColl = function () {
        if (!CalloutAnnotation.$type) {
            CalloutAnnotation.$type = markType(CalloutAnnotation, "CalloutAnnotation");
        }
        var coll = new SyncableObservableCollection$2(CalloutAnnotation.$type, CalloutAnnotation_internal.$type, 0);
        coll.compare = function (ext, int) {
            var comp = ext;
            if (comp._implementation) {
                comp = comp._implementation;
            }
            if (comp.equals) {
                return comp.equals(int);
            }
            return comp === int;
        };
        coll.createTo = function (ext) {
            return ext._implementation;
        };
        coll.createFrom = function (int) {
            var ext = int.externalObject;
            if (!ext) {
                ext = new CalloutAnnotation();
                if (ext) {
                    ext._implementation = int;
                }
            }
            return ext;
        };
        return coll;
    };
    return CalloutAnnotationCollection;
}(IgCollection));
export { CalloutAnnotationCollection };
