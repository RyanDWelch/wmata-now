import { IgCollection } from "igniteui-react-core/ES2015/IgCollection";
import { markType } from "igniteui-react-core/ES2015/type";
import { SyncableObservableCollection$2 } from "igniteui-react-core/ES2015/SyncableObservableCollection$2";
import { Axis as Axis_internal } from './Axis';
import { IgrAxis as IgrAxis } from './igr-axis';
export class AxisCollection extends IgCollection {
    constructor(list) {
        super();
        if (!IgrAxis.$type) {
            IgrAxis.$type = markType(IgrAxis, "IgrAxis");
        }
        if (list) {
            for (let i = 0; i < list.length; i++) {
                this.add(list[i]);
            }
        }
    }
    _createInnerColl() {
        if (!IgrAxis.$type) {
            IgrAxis.$type = markType(IgrAxis, "IgrAxis");
        }
        let coll = new SyncableObservableCollection$2(IgrAxis.$type, Axis_internal.$type, 0);
        coll.compare = (ext, int) => {
            let comp = ext;
            if (comp._implementation) {
                comp = comp._implementation;
            }
            if (comp.equals) {
                return comp.equals(int);
            }
            return comp === int;
        };
        coll.createTo = (ext) => {
            return ext._implementation;
        };
        coll.createFrom = (int) => {
            let ext = int.externalObject;
            if (!ext) {
                ext = IgrAxis._createFromInternal(int);
                if (ext) {
                    ext._implementation = int;
                }
            }
            return ext;
        };
        return coll;
    }
}
