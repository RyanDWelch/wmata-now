import { IgCollection } from "igniteui-react-core/ES2015/IgCollection";
import { SyncableObservableCollection$2 } from "igniteui-react-core/ES2015/SyncableObservableCollection$2";
import { MarkerType_$type } from './MarkerType';
import { ensureEnum } from "igniteui-react-core/ES2015/componentUtil";
export class MarkerTypeCollection extends IgCollection {
    constructor(list) {
        super();
        if (list) {
            for (let i = 0; i < list.length; i++) {
                this.add(list[i]);
            }
        }
    }
    _ensureOuter(item) {
        return ensureEnum(MarkerType_$type, item);
    }
    _createInnerColl() {
        let coll = new SyncableObservableCollection$2(MarkerType_$type, MarkerType_$type, 0);
        coll.compare = (ext, int) => {
            let comp = ext;
            if (comp.equals) {
                return comp.equals(int);
            }
            return comp === int;
        };
        coll.createTo = (ext) => {
            return ext;
        };
        coll.createFrom = (int) => {
            return int;
        };
        return coll;
    }
}
